{{#if isExpanded}}
  {{safeText}}
  {{#if canCollapse}}
    <a href="#" {{action "toggleExpand"}}>{{collapseText}}</a>
  {{/if}}
{{else}}
  {{truncated}}
  {{#if isTruncated}}
    {{#if canExpand}}
      <a href="#" {{action "toggleExpand"}}>{{expandText}}</a>
    {{/if}}
  {{/if}}
{{/if}}